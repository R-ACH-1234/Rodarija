# مسودة برمجة لوحة تحكم مفاتيح API (API Keys Dashboard Prompt)

يمكنك استخدام هذا البرومت (Prompt) في أي نموذج ذكاء اصطناعي (مثل Gemini أو غيره) لإنشاء تطبيق مطابق تماماً لما تم بناؤه:

---

**البرومت (Prompt):**

"قم بإنشاء تطبيق ويب حديث من صفحة واحدة يعبر عن **لوحة تحكم لإدارة مفاتيح واجهة برمجة التطبيقات (API Keys Dashboard)** باستخدام مكتبة React و Tailwind CSS للتصميم، وأيقونات من `lucide-react`.

**يجب أن يشتمل التطبيق على الميزات والمكونات التالية:**

1. **الواجهة الرئيسية (Main Dashboard):**
   - **الرأس (Header):** يحتوي على عنوان التطبيق "API Keys Dashboard" مع أيقونة مفتاح.
   - **القسم العلوي:** عنوان رئيسي مع وصف قصير، وزر بارز لإنشاء مفتاح جديد "Create new key".
   - **نظام التبويبات (Tabs):** زرين للتبديل بين "Active Keys" (المفاتيح النشطة غير المنتهية الصلاحية) و "Inactive Keys" (المفاتيح الملغاة أو منتهية الصلاحية).

2. **جدول المفاتيح (Keys Table):**
   - أعمدة الجدول يجب أن تعرض:
     - **Name:** اسم المفتاح مع عرض النطاقات (Scopes) المرتبطة به تحته على شكل Tags.
     - **Token:** للنوع السري (Secret) يجب إخفاء الجزء الأوسط واستبداله بنقاط (مثل `sk_live_••••cdef`). للنوع العام (Publishable) يمكن إظهاره بالكامل. يجب توفير زر لنسخ المفاتيح الظاهرة.
     - **Type:** يوضح النوع (Publishable بخلفية صفراء/برتقالية، أو Secret بخلفية زرقاء) وبيئة العمل (Test أو Live).
     - **Status:** حالة المفتاح (Active بخلفية خضراء أو Revoked/Expired بخلفية حمراء).
     - **Created:** تاريخ الإنشاء.
     - **Actions:** زر "Revoke" (إبطال) للمفاتيح السرية النشطة فقط.

3. **نافذة إنشاء مفتاح جديد (Create Key Modal):**
   - تظهر عند الضغط على زر "Create new key" مع خلفية مظللة.
   - تحتوي على الحقول التالية (Form):
     - **Name (نص):** اسم المفتاح.
     - **Key Type (Select):** خياران Secret و Publishable.
     - **Environment (Select):** خياران Live و Test.
     - **Expiration (Select):** خيارات: بدون انتهاء (No expiration)، 7 أيام، 30 يوم، 90 يوم.
     - **Scopes (Checkboxes):** خيارات لـ `read`، `write`، و `admin`.
   - زران للإلغاء (Cancel) أو الإنشاء (Create key).

4. **بعد نجاح الإنشاء (Success Alert):**
   - إخفاء النافذة المنبثقة وعرض رسالة نجاح في الواجهة الرئيسية تحتوي على:
     - نَص مُخصص حسب النوع الإشعار: إذا كان المفتاح Secret، أبلغ المستخدم بوجوب نسخة الآن لأنه "لن يظهر مرة أخرى". وإذا كان Publishable اعرض رسالة إنشاء عادية.
     - حقل للقراءة فقط (Read-only) يعرض الرمز (Token) الكامل مع زر "Copy" يقوم بنسخه للحافظة ويتغير نصه إلى "Copied!" لمدة ثانيتين.

5. **المنطق الوظيفي (Logic & State):**
   - استخدام `useState` لإدارة قائمة المفاتيح وتخزين بيانات وهمية مبتدئة (Mock Data).
   - توليد الـ Token بشكل ديناميكي بحيث يبدأ بالبادئة المناسبة (`sk_` للسري و `pk_` للعام) ثم البيئة (`live_` أو `test_`) تليها سلسلة عشوائية.
   - برمجة خاصية النسخ للحافظة (Clipboard API).
   - برمجة زر الإلغاء (Revoke) ليغير حالة المفتاح المستهدف إلى `revoked`.

**المتطلبات التقنية:**
- استخدام TypeScript وكتابة الأنواع المناسبة (Interfaces).
- تصميم متجاوب (Responsive) ونظيف بألوان هادئة مثل درجات الرمادي الكثيفة والنيلي (Indigo)."
